MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_BUCKET=atrofiq

# For CORS; set to your frontend origin in prod (comma-separated list)
ALLOWED_ORIGINS=*

# PostgreSQL (for worklist metadata)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=brain_mri_db
DB_USER=brainuser
DB_PASSWORD=securepassword123
# Optional full URL (overrides above):
# DATABASE_URL=postgresql+psycopg2://brainuser:securepassword123@127.0.0.1:5432/brain_mri_db
